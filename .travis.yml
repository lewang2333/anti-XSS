language: python
python:
  - "2.7"
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script:
  - python anti-XSS.py -h
  - python anti-XSS.py --help
  - python anti-XSS.py -u http://www.google.com
